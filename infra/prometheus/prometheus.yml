global:
  scrape_interval: 15s

scrape_configs:
  - job_name: ishifo-api
    metrics_path: /api/metrics
    authorization:
      type: Bearer
      credentials_file: /tmp/metrics_token
    static_configs:
      - targets: ['api:3001']
